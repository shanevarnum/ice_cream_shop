import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#283040",
    },
    secondary: {
      main: "#00AEEF",
    },
  },
});

export default theme;
